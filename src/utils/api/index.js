module.exports = {
	// Get all Prompts
	getAllPrompts: () => {
		// api call to get all prompts
	},

	// Get trending Prompts
	getTrendingPrompts: () => {
		// api call to get all prompts but in most liked order
	},

	// Get one prompt by id
	getPromptById: () => {
		// api call to get one prmompt by its id
	}
};
